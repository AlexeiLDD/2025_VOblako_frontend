import type { QuickAction } from "./types";

type ViewSwitchersProps = {
  options: QuickAction[];
};

const ViewSwitchers = ({ options }: ViewSwitchersProps) => {
  void options;
  return null;
};

export default ViewSwitchers;
